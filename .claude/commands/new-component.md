Create a new React component for this project.

Component name: $ARGUMENTS

Rules:
- Use TypeScript with explicit Props type
- Use Tailwind CSS for styling (use mobile:/tablet:/laptop: breakpoints, NOT sm:/md:/lg:)
- Client component only if state or browser API is needed — add "use client" at the top
- No default export comments, no unnecessary JSDoc
- Place in the appropriate directory under src/components/
- SVG imports: import Foo from '@/public/svg/...'

Generate the component file content only.
